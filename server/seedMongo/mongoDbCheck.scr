use seedDB;
show collections;
// db.users.find({}, limit=1)
//db.reviews.find({"reviewId" : 1})
db.restaurants.find({}).limit(1);
//db.reviews.createIndex( { reviewId: 1 } );
// mongo < mongoDbCheck.scr

